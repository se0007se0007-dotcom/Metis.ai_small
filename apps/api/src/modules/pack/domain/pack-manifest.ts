/**
 * Pack Manifest — re-exports from @metis/types for backward compatibility.
 * The actual implementation lives in packages/types/src/pack-domain.ts
 */
export type { PackManifest, ManifestValidationResult } from '@metis/types';
export { parseManifest } from '@metis/types';
