# headroom 프록시를 켤 때만 .env에 추가(끄면 줄을 지우거나 비우면 기존 동작):
#   호스트만, 경로(/v1) 없이 — 우리 코드가 /v1/messages, /v1/chat/completions, /v1/embeddings를 붙임
ANTHROPIC_BASE_URL=http://localhost:8787
OPENAI_BASE_URL=http://localhost:8787
