{{- if .Values.secret.create }}
apiVersion: v1
kind: Secret
metadata:
  name: metis-secrets
  labels:
    {{- include "metis.labels" . | nindent 4 }}
type: Opaque
stringData:
  {{- range $k, $v := .Values.secret.data }}
  {{ $k }}: {{ $v | quote }}
  {{- end }}
{{- end }}
