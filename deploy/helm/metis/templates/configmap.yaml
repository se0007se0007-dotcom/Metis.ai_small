apiVersion: v1
kind: ConfigMap
metadata:
  name: metis-config
  labels:
    {{- include "metis.labels" . | nindent 4 }}
data:
  {{- range $k, $v := .Values.config }}
  {{ $k }}: {{ $v | quote }}
  {{- end }}
